/**
 * Настройки заезда. Правьте цифры здесь — остальной код читает их отсюда.
 *
 * Всё, что связано со временем, — в секундах, с топливом — в литрах,
 * с деньгами — в рублях.
 */
export const CONFIG = {
  /** Количество ботов-конкурентов в заезде. Укажите целое число от 0. */
  botCount: 10,

  /** Через сколько секунд проигравший бот возвращается в заезд. */
  botRespawnDelay: 3,

  /**
   * Скорость расхода бензина, л/с — сколько машина сжигает на полном газу.
   * Холостой ход и ручник берут свою долю от этой цифры, так что менять
   * достаточно одно число: 0.57 ≈ полный бак на 90 секунд гонки.
   */
  fuelBurnPerSecond: 0.57,

  /**
   * Базовое время обслуживания машины на АЗС. За это время бак без канистр
   * плавно заполняется до доступного уровня (с учётом денег и лимита колонки).
   */
  stationTimeoutBase: 1,

  /**
   * Дополнительное время обслуживания за каждую канистру у машины.
   * Итого T = stationTimeoutBase + stationTimeoutPerCanister × канистры.
   * По окончании того же T открывается следующая случайная АЗС.
   */
  stationTimeoutPerCanister: 1,

  /** Длительность таймаута билборда после взаимодействия, в секундах. */
  billboardTimeout: 20,

  /** Сколько литров в баке на старте заезда. */
  startFuel: 50,

  /** Объём бака на старте; каждая канистра добавляет к нему свои 10 л. */
  startTankVolume: 50,

  /** На сколько литров канистра увеличивает бак машины. */
  canisterTankBonus: 10,

  /** Сколько рублей у игрока на старте. */
  startMoney: 10000,

  /** По сколько рублей за литр база скупает бензин у игрока. */
  fuelSellPrice: 200,

  /** Нижняя граница цены литра на АЗС — у каждой станции цена своя. */
  stationPriceMin: 70,

  /** Верхняя граница цены литра на АЗС. */
  stationPriceMax: 120,

  /** Сколько литров максимум отпускает АЗС с ограничением за одну заправку. */
  stationFuelLimit: 20,

  /** Доля заправок с таким ограничением: 0.2 — примерно каждая пятая. */
  stationLimitChance: 0.2,
};
